# Retired deployment path

This previous version remains available when the file is deleted by a pull request.

## Legacy sequence

1. Build the package locally.
2. Upload it to the retired staging share.
3. Ask the release owner to promote it manually.

The replacement workflow now lives in `production-syntax.md`.
